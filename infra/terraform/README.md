# Terraform Placeholder

No Terraform is executed or required for the local MVP.

Add Terraform only after the AWS account, region, network topology, domain/TLS strategy, expected traffic, and budget constraints are defined. A safe future baseline should be modular and cover ECR, ECS/Fargate, RDS PostgreSQL, IAM, logging, secrets, and scheduled ingestion.

